name: build

runs:
    using: composite

    steps:
    - name: Install dependencies
      shell: bash
      run: pip install pre-commit ruamel.yaml.clib==0.2.8 ruff

    - name: Lint
      shell: bash
      run: pre-commit run --all
